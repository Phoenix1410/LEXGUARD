# LEXGUARD Vercel Environment Variables Configuration
# Copy these variables to your Vercel Dashboard → Project Settings → Environment Variables

# === REQUIRED FOR ALL DEPLOYMENTS ===

# Clerk Authentication (Required)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZmxleGlibGUtd2FscnVzLTE4LmNsZXJrLmFjY291bnRzLmRldiQ
CLERK_SECRET_KEY=sk_test_gzUTeIfAkfCNOungCUnvMP6Ta5mgDK18iHUbKShomE

# Clerk URL Configuration (Required)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# === OPTIONAL (Advanced Configuration) ===

# DO NOT SET THESE IN PRODUCTION unless you need to override defaults:
# NEXT_PUBLIC_API_URL=http://localhost:8000  # ❌ DON'T SET - breaks production routing
# NEXT_PUBLIC_PRODUCTION_API_URL=https://huggingface.co/spaces/Phoenix1410/LEXGUARD_API  # ⚠️ Override only if needed

# === AUTOMATIC BEHAVIOR ===
# Production deployment will automatically:
# 1. Detect production environment (NODE_ENV=production + non-localhost domain)
# 2. Route AI endpoints to: https://huggingface.co/spaces/Phoenix1410/LEXGUARD_API
# 3. Use HuggingFace Spaces backend for all API calls

# === DEPLOYMENT INSTRUCTIONS ===
# 1. Go to Vercel Dashboard → Your Project → Settings → Environment Variables
# 2. Add the REQUIRED variables above
# 3. Set Environment to "Production" for each variable
# 4. Deploy your project
# 5. Test that AI explanations work in production

# === VALIDATION ===
# After deployment, open browser console and run:
# import { getEnvironmentInfo } from '@/lib/api-config'
# console.log('Environment Info:', getEnvironmentInfo())
# 
# Should show:
# - environment: "production"
# - apiUrl: "https://huggingface.co/spaces/Phoenix1410/LEXGUARD_API"